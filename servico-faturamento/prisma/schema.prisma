generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Pagamento {
  codigo         BigInt   @id @default(autoincrement())
  codAss         BigInt
  valorPago      Float
  dataPagamento  DateTime
}